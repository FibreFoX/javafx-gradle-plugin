language: java

jdk:
  - oraclejdk8

# http://stackoverflow.com/questions/35128777/travis-ci-build-failed
before_install:
  - chmod +x gradlew

branches:
  only:
  - master

notifications:
  email: false
